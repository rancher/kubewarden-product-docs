include::partial$variables.adoc[]
= Desabilitação de emergência
:page-languages: [en, de, es, fr, ja, pt, zh]
:revdate: 2026-06-18
:page-revdate: {revdate}
:sidebar_label: Desabilitação de emergência
:sidebar_position: 31
:title: Desabilitação de emergência
:description: Aprenda como desabilitar o {admc-name} em situações de emergência, parando e excluindo configurações relacionadas, e depois restaurar.
:keywords: ["{admc-name}", kubewarden, ClusterAdmissionPolicies, AdmissionPolicies, configuration]
:doc-persona: kubewarden-operator
:doc-type: howto
:doc-topic: operator-manual

Em uma emergência, as equipes de operações podem precisar realizar ações que {admc-name} bloquearia.

Este documento explica como desabilitar {admc-short-name} para que as equipes de operações possam resolver falhas no cluster. Após resolver a situação, as equipes de operações podem reiniciar {admc-short-name}.

== Desabilitar {admc-short-name}

Primeiro, pare o controlador {admc-short-name}. Você faz isso escalando sua implantação para zero:

[source,console]
----
kubectl scale deployment kubewarden-controller \
  --replicas=0 \
  -n kubewarden
----

[tip]
====
Certifique-se de usar o nome do namespace onde a pilha {admc-short-name} está implantada.
Aqui, a pilha está implantada no namespace `kubewarden`.
====

Em seguida, exclua todos os `ValidatingWebhookConfigurations` e `MutatingWebhookConfigurations` criados por {admc-short-name}:

[source,console]
----
# Delete all the ValidatingWebhookConfiguration created by {admc-short-name}
kubectl delete validatingwebhookconfigurations \
  -l app.kubernetes.io/part-of=kubewarden

# Delete all the MutatingWebhookConfiguration created by {admc-short-name}
kubectl delete mutatingwebhookconfigurations \
  -l app.kubernetes.io/part-of=kubewarden
----

== Restaurar {admc-short-name}

Quando a emergência acabar, restaure a pilha {admc-short-name} trazendo de volta o controlador {admc-short-name}:

[source,console]
----
kubectl scale deployment kubewarden-controller \
  --replicas=1 \
  -n kubewarden
----

[tip]
====
- Certifique-se de usar o nome do namespace onde a pilha {admc-short-name} está implantada.
  Aqui, a pilha está implantada no namespace `kubewarden`.
- Certifique-se de escalar o controlador de volta ao seu valor original. Este exemplo assume que há apenas uma réplica do controlador em execução.
====
Uma vez que o controlador esteja em execução, ele reconcilia as políticas implantadas. Os recursos `ValidatingWebhookConfiguration` e `MutatingWebhookConfiguration`, anteriormente excluídos, são recriados, assim reforçando as políticas.