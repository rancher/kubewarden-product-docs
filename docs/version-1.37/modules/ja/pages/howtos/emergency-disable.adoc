include::partial$variables.adoc[]
= 緊急無効化
:page-languages: [en, de, es, fr, ja, pt, zh]
:revdate: 2026-06-18
:page-revdate: {revdate}
:sidebar_label: 緊急無効化
:sidebar_position: 31
:title: 緊急無効化
:description: 緊急時に{admc-name}を無効化する方法を学び、関連する設定を停止および削除し、その後復元します。
:keywords: ["{admc-name}", kubewarden, ClusterAdmissionPolicies, AdmissionPolicies, configuration]
:doc-persona: kubewarden-operator
:doc-type: HOWTO(操作方法)
:doc-topic: オペレーター・マニュアル

緊急時には、オペレーションチームが{admc-name}によってブロックされるアクションを実行する必要がある場合があります。

この文書では、オペレーションチームがクラスターの障害に対処できるようにするために{admc-short-name}を無効化する方法を説明します。状況が解決した後、オペレーションチームは{admc-short-name}を再起動できます。

== {admc-short-name}を無効化

まず、{admc-short-name}コントローラーを停止します。これを行うには、そのデプロイメントをゼロにスケールします：

[source,console]
----
kubectl scale deployment kubewarden-controller \
  --replicas=0 \
  -n kubewarden
----

[tip]
====
{admc-short-name}スタックがデプロイされているネームスペースの名前を使用していることを確認してください。
ここでは、スタックが`kubewarden`ネームスペースにデプロイされています。
====

次に、{admc-short-name}によって作成されたすべての`ValidatingWebhookConfigurations`と`MutatingWebhookConfigurations`を削除します：

[source,console]
----
# Delete all the ValidatingWebhookConfiguration created by {admc-short-name}
kubectl delete validatingwebhookconfigurations \
  -l app.kubernetes.io/part-of=kubewarden

# Delete all the MutatingWebhookConfiguration created by {admc-short-name}
kubectl delete mutatingwebhookconfigurations \
  -l app.kubernetes.io/part-of=kubewarden
----

== {admc-short-name}を復元

緊急事態が終了したら、{admc-short-name}コントローラーを復元することによって{admc-short-name}スタックを復元します：

[source,console]
----
kubectl scale deployment kubewarden-controller \
  --replicas=1 \
  -n kubewarden
----

[tip]
====
- {admc-short-name}スタックがデプロイされているネームスペースの名前を使用していることを確認してください。
  ここでは、スタックが`kubewarden`ネームスペースにデプロイされています。
- コントローラーを元の値にスケールバックすることを確認してください。この例では、コントローラーのレプリカが1つだけ実行されていると仮定しています。
====
コントローラーが実行されると、デプロイされたポリシーを調整します。以前に削除された`ValidatingWebhookConfiguration`と`MutatingWebhookConfiguration`リソースが再作成され、ポリシーが強制されます。